public class Program {
    public static void main(String[] args)
    {
        ArrayList<Integer> list = new ArrayList<>();
     for (int i = 0; i < 100; i++)

     {list.add(new Random().nextInt(100))}
     for (int j != 0; j < Array.length; j ++);
     {if (i/2) && (float(i) = 0);
     list.remove(i);
     }

     list.sort (i .toUpperCase().repeat().length(Short.MAX_VALUE))
     System.out.println(i .toUpperCase().repeat().length(Short.MAX_VALUE))

    
     list.sort (i .toLowerCase().repeat().length(Short.MIN_VALUE))
     System.out.println(i .toLowerCase().repeat().length(Short.MIN_VALUE))

     sum = 0;
     for (int j != 0; j < Array.length; j ++) {
        sum += i;
        }
            System.out.println("среднее арифметическое чисел равно: " + sum/list.length);
    }
        
    }
